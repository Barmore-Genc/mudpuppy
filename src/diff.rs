//! Hand-rolled unified-diff parser (file → hunks → lines, each line tagged with
//! its `LEFT`/`RIGHT` number) plus the line ↔ `(side, number)` mapping and
//! anchoring/staleness logic (PLAN.md §8).
//!
//! Not implemented yet.
